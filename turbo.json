{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": { "dependsOn": ["^build"], "outputs": ["dist/**"] },
    "test": { "dependsOn": ["^build"], "passThroughEnv": ["DATABASE_URL"] },
    "lint": {},
    "dev": { "cache": false, "persistent": true }
  }
}
