/**
 * Brand identity — single source of truth.
 * Change `name` / `vendor` here to rebrand the whole app.
 * (Product copy lives in i18n messages; the brand name itself is a constant.)
 */
export const BRAND = {
  name: "Outreach",
  vendor: "Kopexa",
} as const;
